import 'package:flutter/material.dart';

class FailState extends StatelessWidget {
  const FailState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Failed to load data'));
  }
}
